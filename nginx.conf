events {}
http {
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            try_files $uri /index.html;
            sendfile on;
            sendfile_max_chunk 1m;
            tcp_nopush on;
        }
    }
}